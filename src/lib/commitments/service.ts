class S{async submit(id,u,b){let c=await this.repo.findById(id);if(!c||c.userId!==u)throw new Error("denied");if(c.status==="submitted"||c.status==="confirmed")return c;let r=await this.repo.update(id,c.version,{status:"submitting"});try{let x=await b(r);return this.repo.update(id,r.version,{status:"submitted",txHash:x.txHash})}catch(e){return this.repo.update(id,r.version,{status:"pending"})}}}